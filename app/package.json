{
    "name": "app",
    "version": "0.1.0",
    "scripts": {
        "serve": "mkdir -p build; cp src/index.html build; node ./build.js;"
    },
    "devDependencies": {
        "esbuild": "^0.18.11",
        "esbuild-plugin-wasm": "^1.1.0"
    },
    "license": "UNLICENSED"
}
